好的，老师。基于 v2 做**精确替换**，不动无关代码。

改动清单：
1. **ThreeJSRenderer**：添加 `platform` InstancedMesh（半高 8px）
2. **新增 PlayerRenderer**：Group 包含身体/头/眼/脚/工具
3. **GameLoop**：每帧更新玩家 Group 位置+朝向
4. **App.init()**：初始化 PlayerRenderer
5. **监视信息**：v2 已有，确保跟随
**下载链接**：[my-terraria_v3.html](sandbox:///mnt/agents/output/my-terraria_v3.html)

---

**v3 改动验证（全部通过，零重复）**：

| 改动点 | 状态 | 说明 |
|--------|------|------|
| **platform InstancedMesh** | ✅ | `BoxGeometry(32, 8, 1)` 半高，中心在 `y*32+4` |
| **buildWorld platform 分支** | ✅ | 独立计数 + 矩阵设置 |
| **updateBlock platform 分支** | ✅ | 挖掘/放置时正确隐藏/显示 |
| **PlayerRenderer Group** | ✅ | 身体/头/眼/双脚/工具，共 6 个 Mesh |
| **PlayerRenderer.init()** | ✅ | 在 `buildWorld()` 后调用，加入 Scene |
| **PlayerRenderer.update()** | ✅ | 每帧更新位置 + 朝向（眼睛/工具镜像翻转） |
| **相机跟随** | ✅ | `Camera.update()` 已存在，玩家居中 |
| **监视信息** | ✅ | `coordsEl` 显示玩家格子坐标 |

**玩家部件坐标（脚底为原点）**：
- 身体：`y=16`（24×32，蓝色）
- 头：`y=40`（20×16，肤色）
- 眼：`y=40, x=±4`（4×4，黑色，根据朝向切换）
- 脚：`y=4, x=±6`（8×8，深蓝）
- 工具：`y=20`（24×6 手柄 + 8×14 头部，根据朝向 rotateZ ±0.3 + scaleX 镜像）

**下一步候选**：
1. **物理引擎**（重力向下、碰撞 AABB、台阶、平台 snap、水中）
2. **高亮框**（鼠标悬停/点击时的方块边框）

你选哪个？